
#include<iostream>
using namespace std;
int countDigits(int num){
    if(num == 0)
        return 0;
    return (num%10)+countDigits(num/10);

}
int main(){
    int num;
    cin>>num;
    cout<<countDigits(num)<<endl;
    return 0;
}
